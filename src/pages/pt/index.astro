---
import LandingPage from '@components/LandingPage.astro';

/**
 * One of three language routes. Astro's i18n routing sets currentLocale from
 * the directory, and every string and every fact is resolved inside
 * LandingPage from that — so the three files stay identical and cannot drift
 * apart in structure.
 */
---

<LandingPage />
